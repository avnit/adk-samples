interface:
  display_name: "Adk Samples"
  short_description: "Repo-specific patterns and workflows for adk-samples"
  default_prompt: "Use the adk-samples repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true